#include <stdio.h>

int main(int argc, char const *argv[])
{
	long int A,B,C;
	scanf("%llu %llu %llu",&A,&B,&C)
	n=s(digito);
	for (int i = 0, int j=n; i <= n; ++i, j--){
		A[i] = digito[j];
	}

	return 0;
}